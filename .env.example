# =============================================================================
# Paperless NGX MCP Server - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   nano .env
# =============================================================================

# -----------------------------------------------------------------------------
# Required Settings
# -----------------------------------------------------------------------------

# URL of your Paperless NGX server
# Do not include a trailing slash
# Examples:
#   PAPERLESS_URL=https://paperless.example.com
#   PAPERLESS_URL=http://192.168.1.100:8000
PAPERLESS_URL=

# API Token for authentication
# Create this in Paperless NGX: Settings → API Tokens → Add Token
# The token needs appropriate permissions for the operations you want to perform
PAPERLESS_TOKEN=

# -----------------------------------------------------------------------------
# Optional Settings (defaults shown)
# -----------------------------------------------------------------------------

# Port that the MCP server listens on
# Change this if port 3000 is already in use on your server
MCP_PORT=3000

# Transport mode
# - http: For remote access (Claude.ai mobile app, etc.)
# - stdio: For local Claude Desktop integration
# TRANSPORT=http

# Host binding
# 0.0.0.0 = listen on all interfaces (required for Docker)
# 127.0.0.1 = localhost only (more secure for local-only access)
# HOST=0.0.0.0
